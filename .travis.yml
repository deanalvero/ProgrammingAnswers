language: android
jdk: oraclejdk8

android:
  components:
    - tools
    - build-tools-28.0.3
    - platform-tools
    - android-28

before_script:
  - chmod +x gradlew
script:
  ./gradlew :android:lint :android:test